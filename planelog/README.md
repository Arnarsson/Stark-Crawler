# PlaneLog CLI

This directory contains a standalone CLI for interacting with My Plane Log.

## Quick start

```bash
cd planelog
npm install
node myplanelog-cli.js --help
```

For full usage and configuration, see `docs/MYPLANELOG_CLI.md`.
